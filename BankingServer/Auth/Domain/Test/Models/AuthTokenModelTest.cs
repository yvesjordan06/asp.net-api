using BankingServer.Auth.Domain.Models;
using Xunit;

namespace BankingServer.Auth.Domain.Test.Models;

public class AuthTokenModelTest
{
    [Fact]
    public void AuthTokenModel_ShouldBeCreated()
    {
     /*   var token = new AuthTokenModel(UserModel.GenerateMockUser());
        Assert.NotNull(token);
        token.GenerateToken();
        Assert.Null(token.Token);*/
    }
}